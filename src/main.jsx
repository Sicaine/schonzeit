import React from "react";
import ReactDOM from "react-dom/client";
import SchonzeitApp from "../schonzeit.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SchonzeitApp />
  </React.StrictMode>
);
